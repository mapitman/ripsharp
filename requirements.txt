# Python requirements for media-encoding
# Install with: pip install -r requirements.txt

# Required for YAML configuration support
pyyaml>=6.0

# Required for online disc and metadata identification
requests>=2.31.0

# For TMDB metadata lookup
tmdbsimple>=2.9.1

# For disc ID calculation (MusicBrainz and AcoustID lookups)
discid>=1.1.1

# Optional dependencies for enhanced features:

# For better CLI progress display and output
rich>=13.0.0

# For tracking FFmpeg encoding progress
ffmpeg-progress-yield>=0.7.1

